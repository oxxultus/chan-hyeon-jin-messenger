
# 🚀 GUI 메신저 (C/GTK 3)

## 🌟 프로젝트 개요

이 프로젝트는 **C 언어**를 기반으로 **GTK 3** 라이브러리를 사용하여 그래픽 사용자 인터페이스(GUI)를 갖춘 멀티스레드 메신저를 구현합니다. 단순 채팅 기능뿐만 아니라 **클라이언트-투-클라이언트(C2C)** 파일 전송 기능을 포함하며, 다중 채팅방을 지원하는 소켓 서버를 구현합니다.

### 주요 기능

  * **다중 채팅방:** 사용자가 방을 생성하거나 기존 방에 입장하여 대화할 수 있습니다.
  * **GUI 클라이언트:** GTK 3를 사용한 사용자 친화적인 채팅 인터페이스.
  * **멀티스레딩:** 클라이언트와 서버 모두 안정적인 동시 접속 및 비동기 통신을 위해 멀티스레딩을 사용합니다.
  * **파일 전송 (C2C):** 채팅 서버를 통해 핸드셰이크(제어 신호)를 수행한 후, 실제 파일 데이터는 클라이언트 간에 직접 전송됩니다.

## 💻 기술 스택

| 구분 | 기술 | 설명 |
| :--- | :--- | :--- |
| **언어** | C | 서버 및 클라이언트 로직 구현 |
| **GUI** | GTK 3 | 클라이언트 애플리케이션의 사용자 인터페이스 |
| **네트워킹** | TCP/IP 소켓 | 안정적인 데이터 전송을 위한 기반 기술 |
| **빌드 시스템** | Make | 서버와 클라이언트의 컴파일 및 빌드 자동화 |

## 🛠️ 개발 환경 및 요구 사항

본 프로젝트는 **리눅스 환경**에서 개발되었으며, 빌드를 위해 다음 패키지가 설치되어 있어야 합니다.

  * **GCC (GNU Compiler Collection)**
  * **GTK 3 개발 라이브러리:** (`libgtk-3-dev` 패키지 등)
  * **`pkg-config`:** GTK 라이브러리 링크 경로를 확보하기 위해 필요합니다.
  * **`curl`:** 클라이언트가 자신의 공인 IP 주소를 획득하는 데 사용됩니다.

## ⚙️ 빌드 및 실행 방법

프로젝트 루트 디렉토리에서 제공된 `Makefile`을 사용하여 서버와 클라이언트를 빌드하고 실행할 수 있습니다.

### 1\. 빌드 (컴파일)

`make all` 명령어를 사용하여 서버 실행 파일(`bin/server`)과 클라이언트 실행 파일(`bin/client`)을 모두 빌드합니다.

```bash
make all
```

### 2\. 서버 실행 (반드시 먼저 실행)

서버는 포트 `8080`에서 클라이언트의 접속을 기다립니다.

```bash
make run-server
```

### 3\. 클라이언트 실행 및 접속

별도의 터미널 창을 열고 클라이언트를 실행합니다. 여러 개의 클라이언트를 실행하여 다중 접속을 테스트할 수 있습니다.

```bash
make run-client
```

-----

## 📌 파일 전송 유의 사항 (C2C)

파일 전송은 클라이언트-투-클라이언트(C2C) 방식으로 이루어지기 때문에, 파일을 보내는 클라이언트(송신자)는 외부 접속을 허용해야 합니다.

  * **포트 포워딩:** 송신자 클라이언트가 공유기 뒤에 있을 경우, **포트 8081**에 대한 **포트 포워딩** 설정이 송신자의 내부 IP 주소로 지정되어 있어야 합니다.
  * **공인 IP 획득:** 클라이언트 프로그램은 접속 시 자동으로 `curl`을 사용하여 자신의 공인 IP를 획득합니다. 이 IP가 다른 클라이언트에게 전달되어 직접 연결에 사용됩니다.

-----

## 🧹 유틸리티 명령어

| 명령어 | 설명 |
| :--- | :--- |
| `make clean` | 생성된 모든 오브젝트 파일, `/build` 디렉토리, `/bin` 디렉토리 및 실행 파일 제거 |
| `make rebuild` | `make clean` 후 `make all` 실행 |
| `make run-server` | 서버 컴파일 후 실행 |
| `make run-client` | 클라이언트 컴파일 후 실행 |